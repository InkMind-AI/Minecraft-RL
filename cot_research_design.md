# CoT for Minecraft：研究设计方案（v1）

> 前置：baseline 已定型——Qwen3.5-9B + 清洗数据 + keep_p=0.7 + focal=0.75 + h29 推理 = 21.5%（easy-ng）。
> 本文档规划在此之上叠加 CoT（显式推理）的训练研究。

## 0. 核心研究问题（论文的科学贡献点）

1. **RQ1（决策点推理）**：推理应该发生在每一步还是只在决策点？我们的 focal 分析发现 56% 的步是重复动作——每步 CoT 既浪费又会在"推理模态"上复刻 no-op 主导问题。假设：**决策点 CoT**（仅在动作切换时生成推理）优于每步 CoT。
2. **RQ2（推理 vs 上下文记忆）**：h29 的 +5.7pp 证明模型受益于状态记忆。文本推理能否**外化**状态跟踪（"已挖 2 铁，还差 1 个"），从而部分替代视觉上下文？实验矩阵：{CoT, 无CoT} × {h3, h29}。
3. **RQ3（验证式推理）**：q35-ns1 的"高频挥空"被 h29 缓解（看到自己动作的后果）。CoT 中显式加入"上一步是否生效"的验证推理，能否在不依赖长窗口的情况下修复此类病态行为？
4. **RQ4（推理的训练方式）**：thought token 是否应该计入 loss（vs 只作为自由推理空间）？continue-SFT（从 21.5% checkpoint 续训）vs 从头训？

## 1. CoT 数据从哪来（三条路线，建议混合）

### 路线 A：状态接地的模板 CoT（faithful，优先做）
- **来源**：MineStudio 原始轨迹的元数据（inventory / position / 附近实体 / 任务进度——需先审计源数据可用性，当前 parquet 只有图+动作，但源头数据集通常带 voxel/inventory 状态）
- **生成**：决策点处，用真值状态渲染结构化推理：
  ```
  Thought: [进度] 已挖到2个铁矿，还需1个 | [感知] 前方左侧有铁矿石 | [决策] 继续靠近并挖掘
  Action: move(-15, 0) and press(w)
  ```
- **优点**：100% 忠实（接地于真值状态，无幻觉）；零标注成本；可精确控制格式
- **缺点**：语言多样性低，可能学成模板复读

### 路线 B：VLM 蒸馏 CoT（diverse，规模做）
- **来源**：强 VLM（Qwen2.5-VL-72B / GPT-4o）看 (帧, 前后动作) 生成推理
- **优点**：语言多样、可覆盖感知描述（"画面中央偏左有一头牛"）
- **缺点**：**事后合理化风险**（生成的理由未必是动作的真实因果）；需要质检
- **关键实践**：用路线 A 的真值状态作为约束条件喂给 VLM（"基于以下状态写推理"），把幻觉压到最低

### 路线 C：自举迭代（RFT/STaR 式，后期做）
- 当前 CoT 模型 rollout easy 任务 → 保留成功轨迹的推理 → 回炉训练
- STRL 基建（CrossAgent/STRL 的 coa RL 管线）可直接复用，这是第二阶段（RL）的事

### 推荐配比
Phase 1 用 **A+B 混合**（A 保底 50% 保忠实，B 加 50% 提多样性）；先在 1-2k 条轨迹上试产 + 人工抽检 50 条（合格线：推理与状态一致率 >90%、与动作一致率 >95%），再全量。

## 2. CoT 格式设计

```
（决策点 t，动作切换时）
Thought: [状态] 背包有3块原木 | [目标] 需要工作台 | [动作理由] 打开合成界面
Action: press(e)
（非决策点 t+1，延续动作）
Action: move(0, 0) and press()
```

- **决策点触发**：动作文本发生变化时（与 focal 的重复检测完全同源——直接复用 `_focal_dropped_turns` 的重复判定逻辑来定位决策点）
- **thought 结构三段式**：状态→目标/进度→动作理由；严格短（≤40 token），保证推理延迟可控
- **推理频率预期**：按 focal 统计（44% 非重复步），每 episode ~88 个决策点 / 200 步 ≈ 44%
- **历史中 thought 的处理**：推理完成后，thought+action 一起进 history（assistant turn 完整保留）；下一决策点的推理可以引用上文（"如前所述，还差1个铁"）
- **token 预算**：30 步训练段 × ~5 决策点 × 40 token ≈ +200 token/段（当前 ~10.5K），MAX_SEQ_LENGTH 无压力

## 3. 训练设计

### 训练方式（关键决策：continue-SFT）
- **从 nf7-c3000 续训**（学习率小、~1000-1500 步）而非从头——三个理由：
  1. 省算力（13.5h vs 全量）
  2. **隔离 CoT 效应**：同一起点的 A/B 直接归因（21.5% → CoT 后），这是论文的主对照
  3. 动作能力已验证，CoT 只需学"推理生成"不用重学"怎么动"
- loss 消融：① thought+action 都计 loss ② 只 action 计 loss（thought 是自由空间）——各训一个
- 数据沿用清洗数据集 + keep_p/focal 全套机制（决策点 CoT 与 focal 天然对齐：focal 降权的恰好是无推理的重复步）

### 推理侧改动（少量）
- 复用 router 的 `fast_iteration_tick` 机制：决策点做完整推理，非决策点直接出动作（省 50%+ 推理调用）
- system prompt 换 CoT 版（框架支持 `SYSTEM_MESSAGE_TAG`，只需新增一个 prompt 文件）
- vllm 侧无需改动；prefix caching 对 thought-in-history 依然有效

## 4. 实验矩阵（论文表格的骨架）

### 主表：CoT × 记忆窗口（RQ2）

| | h3 | h29 |
|---|---|---|
| baseline（无 CoT） | 15.8% | 21.5% |
| CoT（决策点） | ? | ? |

若 CoT+h3 > 21.5%：**推理外化了记忆**（论文最强结论）；若 CoT+h29 > 21.5% 显著：推理与记忆互补。

### 消融表

| 维度 | 取值 |
|---|---|
| 推理粒度 | 每步 / 决策点 / 子目标级 |
| 数据来源 | 模板 / VLM蒸馏 / 混合 |
| thought loss | 计入 / 不计入 |
| 训练起点 | continue / scratch（可选补） |

### 机制分析（复用我们已建立的全套方法）

- CoT 前后的失败轨迹行为统计（no-op 占比、动作多样性、Combat 挥空率）
- **thought 质量指标**：状态忠实率（thought 中的事实 vs 模拟器真值）、动作-推理一致率
- 按类别拆分：Embodied（空间推理）vs Combat（目标跟踪）受益差异
- 验证式推理对"挥空"病态的修复（RQ3 专项： Combat 失败轨迹的攻击命中率对比）

### 对照组（必做，防审稿人）

1. baseline + zero-shot 思维链 prompt（证明"推理必须训出来"）——1 个评测任务即可
2. CoT 训练后推理时抑制 thought（内化测试：模型是否把推理压缩进了隐层）

## 5. 分阶段路线图

| 阶段 | 内容 | 算力 | 产出 |
|---|---|---|---|
| **P0 本周** | ① zero-shot CoT prompt 对照（1 评测）② MineStudio 源数据状态字段审计 ③ system prompt CoT 版设计 | ~5 GPU·h | 确认数据可行性 |
| **P1 第1周** | 试产 1-2k 轨迹 CoT（A+B），人工抽检 50 条 | 标注机时 | 合格数据管线 |
| **P2 第2周** | **pilot 训练**：continue-SFT 1000 步（决策点 CoT，混合数据），50 任务子集快速评测迭代 | 8 GPU × 5h + 评测 | 首个 CoT 信号（≥21.5% 即为正信号） |
| **P3 第3-4周** | 全量训练 + 主表/消融表全套 | 8 GPU × 4-6 训练 | 论文核心数据 |
| **P4 第5周+** | RFT 自举（成功轨迹回炉）/ RL（复用 STRL 基建） | 视 P3 结果 | 增益叠加 |

## 6. 与已有发现的衔接（为什么我们的 baseline 工作恰好为 CoT 铺路）

| baseline 发现 | 在 CoT 研究中的角色 |
|---|---|
| focal 重复检测（56% 步重复） | 决策点定位器——同一套逻辑直接复用 |
| h29 +5.7pp（记忆价值） | CoT-as-memory 假设的实验基础（RQ2 矩阵） |
| Combat 挥空 → h29 缓解 | 验证式推理（RQ3）的机制靶向 |
| 训推一致性方法论 | CoT 格式训练/推理严格对齐的纪律（血泪教训） |
| easy-ng 基准 + 21.5% 基线 | 干净、可复核、统计功效足够的对照平台 |
| GUI 全零（探针在途） | 若 CoT 有效，GUI 多步规划是最有想象力的试金石（暂不列入主线） |

## 7. 风险与预案

| 风险 | 预案 |
|---|---|
| MineStudio 源数据无状态元数据 | 路线 A 降级为"从图像和动作序列启发式提取"，或全走路线 B + 真值约束 |
| VLM 蒸馏的事后合理化 | 状态约束 + 双重抽检（状态一致率、动作一致率两个指标） |
| CoT 推理延迟拖慢 rollout | 决策点触发（44% 频率）+ 40 token 上限 + prefix caching；实测 P2 阶段把关 |
| thought 训练后退化成模板复读 | 路线 B 混入 + 温度采样多样性；监控 thought 的 distinct-n |
| continue-SFT 灾难性遗忘动作能力 | 小学习率 + 混入 20% 原始（无CoT）数据回放 |
