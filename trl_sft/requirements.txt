# transformers>=4.57 / trl>=0.20 required: Qwen3-VL("qwen3_vl") and Qwen3.5
# ("qwen3_5") are only registered in transformers builds >= 4.57.0.dev0, and TRL's
# SFTConfig renamed max_seq_length -> max_length around the same time. Older
# pinned minimums here would silently resolve to versions that can't load
# Qwen3-VL/Qwen3.5 configs at all, or crash on SFTConfig(max_seq_length=...).
#
# PINNED EXACTLY (not >=): unpinned ranges here were observed, on this exact repo, to
# resolve to DIFFERENT transformers/trl releases across separate `pip install` runs
# made only minutes apart (transformers 5.15.0 / trl 1.9.2 in one run, an older release
# without that in another) -- and TrainingArguments.warmup_ratio was REMOVED as an
# __init__ kwarg in one of those releases (consistent with the
# "warmup_ratio is deprecated ... removed in v5.2" warning seen on the older one),
# causing `SFTConfig(warmup_ratio=...)` to raise `TypeError` non-deterministically
# depending purely on job-submission timing. Exact pins make training runs
# reproducible; bump deliberately (and re-verify --debug) rather than letting `pip`
# silently pick up new releases mid-experiment.
#
# `trl` is pinned to the stable PyPI release v1.10.0, which includes the
# tokenization fix (the `_tokenize` helper in `trl/data_utils.py` that always
# passes `return_dict=True`). This fix was previously only available on the
# `main` branch (commit 24db2a8) and was not in any PyPI release at the time
# of the original pinning; v1.10.0 (released 2026-08-13) is the first stable
# release to include it.
#
# Note: v1.10.0's SFTTrainer._save_checkpoint() calls create_model_card() on
# every --save_steps checkpoint. The model-card template contains non-ASCII
# bytes, which can cause UnicodeDecodeError under a C (ASCII-only) locale.
# This is handled by setting `export PYTHONUTF8=1` in the koala submit command
# (infrastructure-level), not by patching library code.
torch>=2.4.0
transformers==5.15.0
trl==1.10.0
# These exact versions are verified to let SFTTrainer/Accelerate perform the sole
# process-level shard of a HF IterableDataset. Do not add a prior
# split_dataset_by_node call in train_sft.py or each process will be sharded twice.
datasets==5.0.1
accelerate==1.14.0
# PINNED EXACTLY: deepspeed>=0.15.0 (an open range) resolved to 0.19.6 (released
# 2026-08-27 19:08 UTC) on two real 8-GPU jobs submitted the next day, which crashes
# at `import deepspeed` itself -- deepspeed/compile/custom_ops/tp_collectives.py
# registers a `@torch.library.custom_op` with a `partition_sizes: list[int]`
# parameter, and this torch build's `infer_schema` rejects that type annotation
# (ValueError: "Parameter partition_sizes has unsupported type list[int]"), before
# any of our training code ever runs. A third 8-GPU job submitted ~17h earlier (same
# day 0.19.6 was cut, just minutes after its release) resolved to an OLDER cached
# deepspeed and ran fine -- i.e. this is exactly the same "unpinned range resolves to
# a different release depending on job-submission timing" failure mode already
# described above for transformers/trl. 0.19.5 (released 2026-08-10, the release
# immediately before 0.19.6) is the newest version confirmed NOT to hit this.
deepspeed==0.19.5
# Optional: --attn_implementation defaults to "sdpa" (no extra install). flash-attn's
# prebuilt wheel was confirmed ABI-incompatible with the koala training image's torch
# build (import-time "undefined symbol"), and building from source needs
# `pip install --no-build-isolation flash-attn` (plain pip install fails to see the
# already-installed torch inside its isolated build env). Only uncomment/install this
# after verifying it actually imports on your target image.
# flash-attn>=2.6.0
wandb>=0.18.0
Pillow>=10.0.0
pyarrow>=17.0.0
s3fs>=2024.0.0
fsspec>=2024.0.0
